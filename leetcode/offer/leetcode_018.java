package leetcode.offer;

public class leetcode_018 {
    public boolean isPalindrome(String s) {
        // 双指针
        int left = 0, right = 0;
        while (left < right) {
            // 从之前的位置开始读取
//            if (s.charAt(left)) {
//
//            }

        }

        return false;
    }

    /*
        得到不同的
     */
    public boolean eq() {
        return true;
    }
}
